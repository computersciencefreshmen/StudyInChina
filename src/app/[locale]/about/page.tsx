import { notFound } from 'next/navigation'
import { Card, LinkButton, PageHero, SectionHeading } from '@/components/ui'
import { getMessages } from '@/i18n/messages'
import { pageMetadata, requireLocale } from '@/lib/site'

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) { const locale = requireLocale((await params).locale) || 'en'; const m = getMessages(locale); return pageMetadata(locale, m.about.title, m.about.intro, 'about') }
export default async function AboutPage({ params }: { params: Promise<{ locale: string }> }) { const locale = requireLocale((await params).locale); if (!locale) notFound(); const m = getMessages(locale); return <><PageHero variant="compact" eyebrow={m.nav.about} title={m.about.title} description={m.about.intro} actions={<a className="atlas-button atlas-button--primary atlas-button--medium" href="https://yanghanyu2023.wixsite.com/henry" target="_blank" rel="noreferrer">{m.about.creatorWebsite} ↗</a>} /><section className="atlas-container atlas-section"><div className="content-grid"><Card accent="vermilion"><h2 className="atlas-card__title">{m.about.mission}</h2><p>{m.about.missionText}</p></Card><Card accent="jade"><h2 className="atlas-card__title">{m.about.data}</h2><p>{m.about.dataText}</p><div className="atlas-card__footer"><LinkButton href={`/${locale}/data-policy`} variant="quiet">{m.footer.dataPolicy} →</LinkButton></div></Card><Card><h2 className="atlas-card__title">{m.about.creator}</h2><p>{m.about.creatorText}</p><div className="atlas-card__footer"><LinkButton href={`/${locale}/contact`} variant="quiet">{m.nav.contact} →</LinkButton></div></Card></div></section><section className="atlas-container atlas-section section-block--tight"><SectionHeading title={m.about.principlesTitle} /><div className="stat-strip"><div className="stat"><strong>Git</strong><span>{m.about.versionedChanges}</span></div><div className="stat"><strong>Official</strong><span>{m.about.sourceLinkedFacts}</span></div><div className="stat"><strong>6</strong><span>{m.about.publicLanguages}</span></div><div className="stat"><strong>0</strong><span>{m.about.noAds}</span></div></div></section></> }
